miyakowa :: top.
hiromiga :: nom.
nanio :: ((q (/) wh) (\) acc).
kaita :: (acc \ (nom \ s)).
to :: (s \ ss).
sinzita :: (ss \ (top \ s)).
unicorn :: n.
teacher :: n.
molly :: np.
alice :: np.
kity :: (i (/) (np (\) i)).
kata :: (ep (/) (np (\) ep)).
smiles :: (np \ s).
s :: (np \ (np / n)).
every :: (np / n).
some :: (np / n).
everyone :: (1((np)1)).
somebody :: (s / (np \ s)).
everything :: ((s / np) \ s).
left :: (np \ s).
teases :: ((np \ s) / np).
likes :: ((1(np \ ((s)1))) / np).
thinks :: ((np \ s) / s).
claims :: ((np \ s) / (0((s)0))).
pictureof :: (n / (0((np)0))).
everybody :: ((s (/) s) (\) (1((np)1))).
dmnd :: (1((n / n)1)).
strange :: (1((n / n)1)).
weird :: ((1((n)1)) / n).
nice :: (n / n).
old :: (n / n).
rel :: ((s (/) (n \ n)) (\) np).
a :: (a (\) s).
c :: ((s / s) (/) c).
b :: ((s / s) (/) (s (\) (a (\) (s (/) c)))).
suspects :: ((np \ s) / (s / (s \ s))).
somesome :: ((s (/) s) (\) np).
cheats :: (np \ s).
